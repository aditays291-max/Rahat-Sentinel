pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}

rootProject.name = "Rahat"

include(":app")

includeBuild("../node_modules/@react-native/gradle-plugin")
